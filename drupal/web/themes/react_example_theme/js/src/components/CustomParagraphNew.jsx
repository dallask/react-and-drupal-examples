import React from "react";
import CustomParagraphListNew from './CustomParagraphListNew';

const CustomParagraphNew = ({ id, body }) => (
  <CustomParagraphListNew
    id={id}
    body={body}
  />
);

export default CustomParagraphNew;
