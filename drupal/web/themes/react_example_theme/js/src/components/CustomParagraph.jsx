import React from "react";
import CustomParagraphList from './CustomParagraphList';

const CustomParagraph = ({ id, body }) => (
  <CustomParagraphList
    id={id}
    body={body}
  />
);

export default CustomParagraph;
